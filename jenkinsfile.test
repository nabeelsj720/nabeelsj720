pipeline{
      agent any
      stages{
            stage("git checkout")
            {
             steps{
                 script
                 {
                  def a='Hello world';
                  echo "${a}"
                  }
                  }

              }
              }
}
