pipeline{
      agent any
      stages{
            stage("build"){
            steps{
                  def a='Hello world';
                  echo "${a}"
                  }
              }
              }
}
