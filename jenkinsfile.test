pipeline{
      agent any
      stages{
            stage("build")
            {
             steps{
                 script
                 {
                  def a='Hello world';
                  echo "${a}"
                  }
                  }

              }
              }
}
